# Server-only Spring API URL; do not use NEXT_PUBLIC_ for server credentials.
BACKEND_URL=http://127.0.0.1:8080
# Optional: explicitly choose the offline frontend preview before building.
# NEXT_PUBLIC_DATA_MODE=mock
